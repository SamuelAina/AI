// Define the URL endpoint
const actionUrl = 'https://api.example.com/data';

// Define the parameters you want to pass (can be null if no parameters are needed)
const params = {
    key1: 'value1',
    key2: 'value2'
};

// Define an error handler function (optional)
function handleError(error) {
    console.error('An error occurred:', error);
}

// Call the getServiceDataJSON function and handle the result
getServiceDataJSON(actionUrl, params, handleError)
    .then(data => {
        // Process the data returned from the server
        console.log('Data received:', data);
        // You can add more logic here to work with the received data
    })
    .catch(error => {
        // Handle any unexpected errors that occur during the fetch process
        console.error('Unexpected error:', error);
    });
